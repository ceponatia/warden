{
  "name": "warden",
  "version": "0.1.0",
  "private": true,
  "description": "Scaffold the Warden repo and build the data collection layer: shell/Node scripts that gather git stats, staleness data, and maintenance debt markers into structured JSON snapshots.",
  "type": "module",
  "bin": {
    "warden": "./src/cli/index.ts"
  },
  "scripts": {
    "warden": "tsx src/cli/index.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@eslint/js": "^9.21.0",
    "@types/node": "^22.13.10",
    "eslint": "^9.21.0",
    "tsx": "^4.19.2",
    "typescript": "^5.8.2",
    "typescript-eslint": "^8.25.0",
    "vitest": "^3.0.8"
  }
}
